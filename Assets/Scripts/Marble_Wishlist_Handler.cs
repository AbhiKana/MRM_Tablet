using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class Marble_Wishlist_Handler : MonoBehaviour
{
    /*[SerializeField] List<ScanData> scanData;

    public void OnScanResult(string id, Toggle toggle)
    {
        toggle.onValueChanged.AddListener((isOn) =>
        {
            *//*if (isOn && !selectedIds.Contains(id))
                selectedIds.Add(id);
            else if (!isOn && selectedIds.Contains(id))
                selectedIds.Remove(id);*//*
        });
    }*/
}
